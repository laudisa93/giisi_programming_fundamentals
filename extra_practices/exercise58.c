#include <stdio.h>
/*Escriba un programa que busque un entero en un array unidimensional de enteros mediante un algoritmo de búsqueda
binaria mostrando en la pantalla la posición en la que se encuentra. El algoritmo de búsqueda binaria requiere que
el vector se encuentre ordenado y consiste en lo siguiente. Se toma el elemento central del vector y se compara con
el valor buscado. Si el elemento central coincide con el buscado, ya hemos acabado. Si no, si el valor buscado es
menor, tomamos el subvector que va desde el elemento central al principio. Si el valor buscado es mayor, tomamos el
subvector que va desde el elemento central hasta el final. Así se vuelve a aplicar este procedimiento con el nuevo
subvector, tomando el elemento central del nuevo subvector y repitiendo los pasos anteriores. Procedemos de esta
manera con intervalos cada vez menores hasta que, o bien lleguemos a un intervalo indivisible, en cuyo caso el
elemento no está en el vector, o el elemento central sea nuestro elemento.*/

void main(){
    int array[];



}
