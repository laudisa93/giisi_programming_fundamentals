#include <stdio.h>

void main(){
    int num, a, i, primo;
    printf("Intoduzca numero:\n");
    scanf("&d",&a);
    for(num=0;num<=a;num++){
    //num es primo?
    primo=1;
    i=2;
    while(i<=)
    }
}
