#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    P5 (60 min).
    a) Escriba una función que permita leer desde teclado tablas bidimensionales de enteros de tamaño arbitrario (hasta
    un máximo). Dicha función recibirá como argumento una tabla bidimensional en la que se almacenarán los valores leídos, y pasará
    dos argumentos enteros por referencia que servirán para indicar al programa las dimensiones de la tabla leída. Dicha función
    deberá proceder primero preguntando al usuario el número de filas y el número de columnas de la tabla, posteriormente
    comprobará que las dimensiones indicadas están dentro del máximo admitido por el programa (que será definido usando
    constantes simbólicas y será empleado para declarar el tamaño de la tabla que se pase como argumento), y posteriormente
    procederá a pedir al usuario el valor de cada uno de los elementos de dicha tabla.
    b) Desarrolle un programa de prueba que demuestre el funcionamiento de la función anterior. Para mostrar la tabla bidimensional en
    la pantalla use la función desarrollada en el apartado a) del Problema 1.
    */

}




